---
'@hina-ui/vue': minor
---

Add `positionerClass` to HoverCard for styling the outer positioning element independently of the card content. This enables custom movement transitions when switching external anchors while preserving the existing enter and exit animations.
