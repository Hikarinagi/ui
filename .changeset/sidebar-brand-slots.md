---
'@hina-ui/vue': minor
---

Add Sidebar `icon` and `wordmark` slots for a default brand header. Icons fit a fixed square box and remain visible in rail form; wordmarks fade out automatically while preserving the header height. Wordmark-only headers fade out in full, and a custom `header` takes priority over both brand slots.
