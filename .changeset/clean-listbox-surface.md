---
'@hina-ui/vue': minor
---

Add `variant="bare"` to Listbox for embedding inside an existing Card, Sheet or Popover surface. The bare root has no background, border, shadow or corner radius, while option styling, selection and scrolling are preserved. The default remains `primary`.
