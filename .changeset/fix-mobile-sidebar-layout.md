---
'@hina-ui/vue': patch
---

Fix the AppShell mobile sidebar to fill the drawer height, keeping the header and footer fixed while navigation scrolls independently. Remove the duplicate visible drawer title and place the close button in the sidebar header; `mobileTitle` remains the accessible name.
