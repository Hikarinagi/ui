---
type: added
scope: QRCode
---

Add SSR-rendered SVG QR codes with error correction, quiet zones, custom colors and logos, localized loading and expiry states, a custom status slot, and standalone SVG or PNG export. Keep encoding and visual contracts in the shared layer.
