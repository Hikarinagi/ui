---
type: fixed
scope: DataTable
---

Resolve constrained column widths against the container before hydration, preserving width limits, pinned columns and explicit overrides without an initial layout jump. Keep resizing and responsive width allocation consistent with the initial layout.
