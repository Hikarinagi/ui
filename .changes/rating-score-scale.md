---
type: added
scope: Rating
---

Add a `stars` prop to separate the displayed star count from the maximum score. Pointer and keyboard selection, read-only fill, accessible labels, and form values use the configured score scale. Existing ratings keep their current behavior when `stars` is omitted.
