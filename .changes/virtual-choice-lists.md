---
type: added
scope: Select, MultiSelect, Combobox, MultiCombobox, Listbox, CommandPalette
---

Add optional virtualize support with measured row heights and configurable estimates and overscan. Keep search, grouped labels, keyboard navigation, disabled-item skipping and selection working across the complete dataset. Preserve focused rows and native form values without rendering every option, including when dropdowns are closed.
